# 📋 개별 작업 지침서: 초안 자동 저장 및 복원 (TASK-022, TASK-023)

**작업 상태**: 대기 중  
**선행 작업**: `[TASK-063]`, `[TASK-020]`  
**후속 작업**: `[TASK-024]`

---

## 0. 현재 코드 상태와 이 작업의 위치

- **현재 상태 요약**: 메모리(Zustand) 상에는 데이터가 존재하나, 브라우저를 닫으면 모든 입력 내용이 유실됩니다.
- **이 작업의 책임**: 착수나 메타데이터 입력이 발생할 때마다 `localStorage`에 상태를 동기화하고, 앱 재진입 시 이를 자동으로 복구하는 로직을 구현합니다.

## 🎯 1. 작업 목표

- **최종 상태**: 사용자가 실수로 페이지를 새로고침하더라도, 마지막으로 입력하던 보드 상태와 코멘트가 그대로 유지됩니다.

## 🛠️ 3. 상세 기술 사양

- **자동 저장(Auto-save)**: 
  - Zustand의 `subscribe` 기능을 활용하여 상태 변경 시마다 `localStorage.setItem` 호출.
  - 디바운스(Debounce)를 적용하여 과도한 쓰기 방지 (선택 사항).
- **복원(Restore)**: 
  - 앱 초기화(`useEffect`) 시 `localStorage`에서 데이터를 읽어와 Zustand `set` 함수로 복구.
  - `shared`의 Zod 스키마를 사용하여 읽어온 데이터의 유효성 검사 필수.

## ✅ 4. 완료 판정 체크리스트

- [ ] 수 이동 또는 코멘트 작성 후 새로고침 시 데이터가 복구된다.
- [ ] 비정상적인 데이터가 로컬 스토리지에 있을 경우 에러 없이 초기화 상태로 대체된다.


## 💬 9. 추천 커밋 메시지

- `feat: 초안 자동 저장 및 복원 (TASK-022, TASK-023)`
