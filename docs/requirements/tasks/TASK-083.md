# TASK-083 apps/web React + Vite 프로젝트 초기화 및 기본 환경 구성

## 상위 Feature

- [FEATURE-002 기보 입력 보드 상호작용 및 수순 관리](../features/FEATURE-002.md)

## 목적

- 프론트엔드 애플리케이션의 핵심 기반이 될 React + Vite 프로젝트를 생성하고 기본 구조를 잡는다.

## 완료 조건

- [ ] `apps/web` 경로에 `vite`를 사용한 React + TypeScript 프로젝트가 생성되어야 한다.
- [ ] 루트 `tsconfig.base.json`을 상속받는 `apps/web/tsconfig.json`이 설정되어야 한다.
- [ ] `package.json`에 `dev`, `build`, `lint` 스크립트가 정의되어야 한다.

## 참고 사항

- 이 단계에서는 아직 UI 라이브러리를 설치하지 않는다.


## 💬 9. 추천 커밋 메시지

- `chore: update TASK-083.md`
